# Environment
.env
.env.local
.env.*.local
.env.production
.env.staging

# Dependencies
node_modules/
package-lock.json

# Guard / tooling cache (mtime index)
.cache/

# Build
dist/
build/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Keys / Certificates
*.key
*.pem
*.secret
*.p12

# Coverage
coverage/
.nyc_output/

# Temp
tmp/
temp/
*.tmp

# Saídas de gate/E2E/YALA geradas em runs locais (ruído — não versionar).
# Achado do laudo auditoria.md ("drift documental na raiz": dezenas de *_output.txt untracked).
output_*.txt
gate*_output.txt
e2e_test_output.txt
yala_*.txt
*_e2e.txt

# CP-5 identity exports (PII / operação local)
artifacts/identity-cp5-a2-export*.csv








# Temporary backups
*.bak

# Estado operacional do seed de demonstração — ids que o seed criou, para a faxina apagar POR ID
# (Lei 7: identidade é o id). É estado LOCAL da máquina, não código; cada banco tem o seu.
.demo-seed-manifest.json
