# C.25 — Job CI do gate C.22

**Data:** 2026-04-11  
**Workflow:** `.github/workflows/canonical-gates.yml`

## Conteúdo

- `pull_request`
- `pnpm install --frozen-lockfile` na **raiz** do monorepo (lockfile partilhado)
- `pnpm --filter unificard-backend run check:canonical-gates`

## Âmbito

Apenas **anti-regressão C.22** (`backend/scripts/gate-canonical-sql-patterns.mjs`). **Não** inclui inferência / **C.25_SPEC** §1–3 (permanecem rascunho até lista fechada).

## Verificação local (análogo ao job)

```text
pnpm --filter unificard-backend run check:canonical-gates
→ exit 0
```

## Nota

Validação de sintaxe YAML no GitHub Actions ocorre no primeiro PR que tocar o ficheiro ou ao correr o workflow.
