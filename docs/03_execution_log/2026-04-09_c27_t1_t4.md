# C.27 — matriz T1–T4 (evidência)

**Data:** 2026-04-09  
**Ficheiro:** `backend/tests/integration/canonical-resolution.test.ts`  
**Comando:** `cd backend && pnpm run test:integration:canonical-resolution`

## Resultado

**PASS** (4 testes) com `DATABASE_URL` e schema `canonical_products.scope`.

| Teste | Contrato |
|-------|-----------|
| T1 | C.11 — `concept_ref` do canónico global quando o produto referencia o global com scoped duplicado (mesmo GTIN). |
| T2 | C.20 + C.2 — `listAvailableCatalogProducts` deduplica GTIN (scoped sobre global); `findByTenantAndGtin` → scoped. |
| T3 | C.18 — canónico inexistente no contexto do tenant → `failed`, `failureReason` definido (`PRODUCT_NOT_FOUND`). |
| T4 | C.5 / 5C — falha com `failureReason` definido. |

## `pnpm test` (suíte completa)

**FAIL** global do workspace Jest (2026-04-09): erros de compilação TS pré-existentes em `tests/financial-chaos/*` (`CreateBankTransactionInput` — faltam `referenceType`, `referenceId`). **Não** bloqueia o fecho C.27; correcção = PR separado nos testes financeiros.
