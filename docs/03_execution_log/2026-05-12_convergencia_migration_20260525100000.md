# Convergência mecânica runtime ↔ migration soberana 20260525100000 (Commit A1, escopo reduzido)

**Data:** 2026-05-12
**Modo:** EXECUTOR (autorizado por Clayton — FASE 2 do plano de convergência)
**Branch:** `rescue-structural`
**Commit anterior:** `6c8cf6a6` (housekeeping institucional)
**Migration soberana de referência:** `20260525100000_events_domain_and_financial_execution.sql` (cita `07_NOMENCLATURA_CANONICA §4.38` + `SSOT_REGISTRY_UNIFICARD`)

---

## 1. Contexto

Migration `20260525100000` separou:
- **`events`** — lifecycle puro (`draft`, `declared`, `published`, `active`, `ended`, `cancelled`)
- **`event_financial_execution`** — execução financeira pós-evento (lifecycle próprio: `pending`, `running`, ...)

Schema do banco vivo está conforme. HEAD do código mantinha drift residual em pontos que ainda referenciavam `events.completed_at`, `events.split_processed`, e `status='completed'` — estados/colunas inexistentes contra o schema soberano.

A Investigação 5 (`executei_12.md`) mapeou 14 hits em 4 camadas e classificou-os por intenção semântica:
- **(a) lifecycle mecânico** — drift de literal/coluna direto contra schema soberano (4 hits: este Commit A1)
- **(b) financial execution** — código já no lado certo do schema (sem ação)
- **(c) cross-layer ambíguo** — vocabulário paralelo (`'finished'`, `CLOSED`, UPPERCASE) cristalizado em tipos e mappers entre core/modules/contracts/frontend (7 hits — pendente DECISION-0034)

## 2. Prova §2.2.2

- **Documentos lidos:**
  - `executei_12.md` (mapeamento dos 14 hits)
  - `backend/migrations/20260525100000_events_domain_and_financial_execution.sql` (schema soberano)
  - `backend/src/core/events/event.types.ts` (tipo `EventStatus` canônico)
  - Contexto ±50 linhas em cada guard antes de substituir
- **SSOT operacional:** `07_NOMENCLATURA_CANONICA §4.38` + migration soberana
- **Pilar afetado:** Lifecycle de eventos (semântica de status). NÃO toca causalidade financeira (`event_financial_execution` permanece intocado neste commit).
- **Modo:** EXECUTOR (autorização explícita de Clayton com escopo de 4 arquivos; A1.4b deferido por sinalização TSC)

## 3. Escopo reduzido (3 arquivos vs 4 originais)

### 3.1 `backend/src/jobs/event-scheduler.ts`

Working tree completo (autoria institucional mista — incluído conforme autorização):

- **Comentário do método `processEndedEvents` (linha 19-23):** atualizado para refletir `events.status='ended'` + `event_financial_execution` separado.
- **Mudanças pré-existentes** (de sessão anterior): query troca `events.completed_at IS NOT NULL` por `event_financial_execution`, lifecycle alinhado ao schema soberano. Toda a divergência converge ao destino correto.
- **Linha 257** (de session anterior dentro deste commit): `status = 'PENDING'` → `status = 'pending'` (alinhamento §4.11 + DT-C36).

### 3.2 `backend/src/core/events/event.service.ts:128-138` — BUG ALTA fixed

`validateStatus` rejeitava 3 valores canônicos do CHECK vigente (`'declared'`, `'active'`, `'ended'`). Lista expandida para 8 valores:

- 6 canônicos do CHECK (`draft`, `declared`, `published`, `active`, `ended`, `cancelled`)
- 2 legacy aceitos como compatibilidade transitória (`completed`, `archived`) durante convergência gradual ao vocabulário canônico (norma assintótica §25)

### 3.3 `backend/src/core/events/event.service.ts:340-344` — guard `updateEvent`

Guard de lifecycle encerrado (`'completed' || 'archived'`) substituído pelos canônicos do CHECK (`'cancelled' || 'ended'`). Validação semântica: contexto do método (`updateEvent`) confirma que representa lifecycle encerrado, NÃO execução financeira. Dead branches contra schema soberano eliminados.

## 4. Hit deferido — A1.4b (`modules/events/events.service.ts:521`)

**TSC sinalizou** (TS2367): o guard herda tipo `EventStatusForModule = 'draft' | 'completed' | 'archived' | 'published' | 'finished'` — **vocabulário paralelo cristalizado** no cluster (c) ambíguo cross-layer, não no canônico.

**Diagnóstico:** o guard é um **ponto de costura entre clusters (a) e (c)** — semanticamente lifecycle (a), tipograficamente sob domínio do tipo paralelo (c). A Investigação 5 classificou pelo literal; o tipo de origem revelou o cluster real ao qual pertence.

**Decisão institucional:** DEFERIR para sessão dedicada de DECISION-0034 (cluster cross-layer com 5+ hits em core/modules/contracts/frontend). Resolver agora exigiria invasão do cluster (c) — fronteira "paro e consulto" honrada.

**Não é defeito de implementação.** O TSC funciona aqui como sinal arquitetural correto, não obstáculo a contornar (anti-padrão §28).

## 5. Verificação

```bash
cd C:/unificard/backend && npx tsc --noEmit
# EXIT=0
```

4 gates institucionais a rodar pós-commit:
- `validate:actor-writer-boundaries`
- `validate:bank-ledger-boundaries`
- `validate:regression-guards`
- `validate:architecture`

## 6. Anti-padrões fechados

- **§4-C** (schema vivo > convenção esperada): código convergiu para o que o banco aceita HOJE.
- **§28** (código atrás de migration soberana): drift residual eliminado nos 3 pontos do core que herdavam tipo canônico.

## 7. Anti-padrões evitados

- **Cleanup destrutivo disfarçado de padronização:** A1.4b foi deferido em vez de mascarado via cast.
- **Silenciar TSC** com `as string` ou ajuste oportunista do tipo paralelo (cluster (c) intocado).

## 8. Estado pós-commit

| Item | Estado |
|---|---|
| `events.completed_at` referências em código | Eliminadas (scheduler convergiu) |
| `events.split_processed` referências em código | Eliminadas (scheduler usa `event_financial_execution`) |
| `validateStatus` rejeita canônicos do CHECK | Eliminado (8 valores aceitos) |
| Guard `updateEvent` em lifecycle (core) | Convergido (`'cancelled' \|\| 'ended'`) |
| Guard `checkIn` (modules) | DEFERIDO — DECISION-0034 |
| Cluster (c) cross-layer | INTOCADO (preservado para sessão dedicada) |
| TSC backend | 0 erros |

## 9. Aprendizado registrado em memória institucional

Padrão **"ponto de costura entre clusters"** registrado como `feedback_costura_clusters.md`: investigações futuras devem classificar hits por DUAS dimensões (semântica do literal + cluster do tipo de origem). Quando há divergência, defer para o cluster mais conservador. TSC como sinal arquitetural quando tipos paralelos cristalizados estão envolvidos.

## 10. Aderência ao protocolo

- ✅ Modo EXECUTOR declarado (autorização Clayton)
- ✅ §2.2.2 prova de rastreabilidade
- ✅ Validação semântica obrigatória executada antes de substituir guards
- ✅ TSC validado pós-edits + reverção
- ✅ PARO E CONSULTO ativado em A1.4b conforme restrição transversal
- ✅ §-1.5 — não bloqueia runtime (lifecycle); reversível; observabilidade preservada
- ✅ §7 — log institucional criado (este arquivo)
- ✅ §10 — não tocou norma
- ✅ Diretiva mestre §2 — fronteira "paro e consulto" honrada
- ✅ §25 — norma assintótica aplicada (legacy aceito como compatibilidade transitória, drift cluster (c) preservado para DECISION dedicada)
- ✅ §28 — anti-padrão "código atrás de migration soberana" diagnosticado e fechado nos 3 pontos do escopo reduzido

---

**FIM DO LOG. Commit A1 (escopo reduzido, 3 arquivos) fechado. Hit #4 + cluster (c) cross-layer aguardam DECISION-0034 dedicada.**
