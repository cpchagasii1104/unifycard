# Fase 5C — Contrato de erro (`failureReason` / `reason`) + continuidade C.28 / C.17 / C.25

**Data:** 2026-04-10  
**Plano:** `PRODUTO_PLANO_MESTRE_COMPLETO.md` §5C, C.5, C.16  
**Orientação:** `ORIENTACAO_PRODUTO_EXECUTAR.md` (Bloco 4 / 5C)

---

## 5C — STATUS: **PASS**

### Ações executadas

1. **`ConceptRefFailureReason`** exportado em `backend/src/modules/marketplace/adapters/concept-offer-refs.adapter.ts` (7 literais alinhados ao plano: `CANONICAL_ROW_MISSING`, `CANONICAL_NOT_READY`, `CONCEPT_ID_NULL`, `SCOPE_TENANT_MISMATCH`, `PRODUCT_MISSING_CANONICAL_ID`, `PRODUCT_NOT_FOUND`, `VARIANT_NOT_FOUND`).
2. **`ConceptOfferRefs.failureReason`** opcional; funções **`resolveRefsFromVariant`**, **`resolveRefsBatchFromVariants`**, **`resolveConceptFromProduct`** propagam causas (incl. diagnóstico auxiliar `diagnoseCanonicalPointer` para `CANONICAL_ROW_MISSING` vs `SCOPE_TENANT_MISMATCH`).
3. **`intent-execute.routes.ts`:** respostas `REF_RESOLUTION_FAILED` incluem **`reason: refs.failureReason ?? 'UNKNOWN'`** (repeat order + fluxo normal com `items`).
4. **`navigation.routes.ts`:** log `failure_reason` + campo **`failure_reason`** no item quando `resolution !== 'ok'`.
5. **Testes:** `backend/tests/unit/concept-offer-refs.adapter.test.ts` — casos por causa (VARIANT, PRODUCT, CANONICAL_*, SCOPE_*, CONCEPT_ID_NULL, OK) + lote.

### Evidências

**Jest (5C):**

```text
pnpm test -- --runTestsByPath ./tests/unit/concept-offer-refs.adapter.test.ts --runInBand --forceExit
→ Test Suites: 1 passed | Tests: 11 passed
```

**Grep — adapter (`ConceptRefFailureReason` / `failureReason`):** ocorrências em `concept-offer-refs.adapter.ts` ≥ 5 (cumprimento checklist plano).

**Grep — RLS em `canonical_products`:** `backend/migrations` — **sem** `CREATE POLICY` / `ROW LEVEL` específico para `canonical_products` (C.28: **N/A estrutural** neste repo; visibilidade continua por SQL em aplicação + `runQueryWithTenant`).

**C.17 (pipeline Postgres real):** `DATABASE_URL` **não definido** neste ambiente de execução → E2E `pnpm test:e2e:catalog` **não corrido** aqui; checklist C.17 permanece `[ ]` no plano até evidência com BD.

**C.25:** sem nova automação CI (conforme plano: spec / lista fechada em `C.25_SPEC.md`); apenas registo de estado **inalterado** — placeholder.

### OpenAPI / contrato HTTP

- Campo **`reason`** acrescentado ao corpo de erro `REF_RESOLUTION_FAILED` na rota POST intent execute.  
- **`backend/docs/API_CONTRACT_GOVERNANCE.md`:** não actualizado neste passo (N/A explícito se rota ainda não catalogada lá).

---

## C.28 — Verificação RLS / globais

**Resultado:** **N/A (evidência negativa)** — não há política RLS nomeada em migrations para `canonical_products`. Recomendação futura: quando RLS for introduzido, repetir teste documentado no plano (SELECT/JOIN com `scope='global'`).

---

## C.17 — Pipeline real

**Resultado:** **PENDENTE neste log** — requer `DATABASE_URL` + migrations aplicáveis; comando sugerido: `pnpm test:e2e:catalog`.

---

## C.25 — Script / CI inferência

**Resultado:** **sem alteração** — não implementado job novo (alinhado a restrições v1.9 / `C.25_SPEC`).

---

## Ficheiros alterados (delta)

| Ficheiro | Alteração |
|----------|-----------|
| `backend/src/modules/marketplace/adapters/concept-offer-refs.adapter.ts` | Enum, classificação, batch SQL alargado, `failureReason` |
| `backend/src/core/intent/intent-execute.routes.ts` | `reason` no erro |
| `backend/src/core/navigation/navigation.routes.ts` | `failure_reason` em log + payload |
| `backend/tests/unit/concept-offer-refs.adapter.test.ts` | **novo** — testes 5C |

---

## Resultado final

| Gate | Status |
|------|--------|
| 5C | **PASS** |
| C.28 (neste âmbito) | **N/A** |
| C.17 | **não exercitado** (sem `DATABASE_URL`) |
| C.25 | **inalterado** |
