# C.17 — Validação de pipeline (catálogo → intent → ledger)

**Data:** 2026-04-10  
**Ambiente:** Postgres local (`DATABASE_URL` definido; host mascarado em logs da suíte como `localhost:5432/unificard_dev`).

## Comandos

```bash
cd backend
pnpm migrate
pnpm test:e2e:catalog
```

**Nota técnica:** o invocador Jest com path directo falhou com `testPathPatternToRegExp is not a function`; o script `test:e2e:catalog` em `backend/package.json` foi corrigido para usar **`--runTestsByPath`**, que executa a suíte sem esse erro.

## Resultado

**PASS**

## Evidência (critérios do plano)

| Critério | Estado |
|----------|--------|
| `canonical_products` READY + produto com `canonical_product_id` | ok (setup do teste) |
| `concept_ref` via adapter (`resolveRefsFromVariant` no ficheiro) | ok |
| `intent.execute` sem `REF_RESOLUTION_FAILED` | ok |
| Ledger / movimento bancário associado ao pedido | ok (`transaction_created` no output) |

## Output (resumo)

- `Test Suites: 1 passed`
- `Tests: 1 passed` — `resolveRefs ok, intent.execute without REF_RESOLUTION_FAILED, ledger rows for order`

## Observação (histórico — resolvida 2026-04-11)

~~Durante `bankTransactionService.transfer`, o runtime emitiu erro consultando `authority_roots` (tabela ausente).~~

## `authority_roots` — **RESOLVIDO**

| Campo | Valor |
|-------|--------|
| **Acção** | **(A)** Migration `backend/migrations/20260514100000_authority_roots.sql` (`CREATE TABLE IF NOT EXISTS authority_roots`, colunas alinhadas ao uso em `authority-decision.service.ts` / arquivo legado `migrations_archive/0985_authority_roots.sql`). |
| **Justificativa** | Tabela faz parte do contrato ATL camada 1; ausência gerava `42P01` e `console.error` em `pool.ts` antes do `catch` `ATL_SCHEMA_ABSENT`. |
| **Re-execução** | `pnpm migrate` + `pnpm test:e2e:catalog` — **sem** linha `authority_roots` / `Database query error` no output (2026-04-11). |
