# Matriz C.17.1 — Bloco 5 (evidência por linha)

**Data:** 2026-04-11  
**Referência normativa:** `ORIENTACAO_PRODUTO_EXECUTAR.md` (tabela C.17.1), `PRODUTO_PLANO_MESTRE_COMPLETO.md` §C.17.1.

---

## Item 1 — **C.11** (ID global; não trocar por scoped)

**Status:** **PASS (implementação + revisão 2B)**  
**Evidência:**

- `docs/03_execution_log/2026-04-10_bloco2_global.md` — inventário C.22 / adapter / `sqlCanonicalIdMatchesTenantContext`.
- `docs/01_normative/LEI_DE_COERENCIA_SISTEMICA_UNIFICARD.md` §4.10.3 — proibição de substituir ID global.
- `backend/src/modules/marketplace/adapters/concept-offer-refs.adapter.ts` — resolução por UUID em `canonical_product_id` + predicado READY.

**Teste automatizado dedicado** (GTIN duplicado scoped+global, assert `concept_ref` = global): **não existe** neste repositório. **Justificativa:** fora do escopo desta execução; lacuna registada como melhoria futura, não como falha de código detectada.

---

## Item 2 — **C.2 Escopo B** (descoberta GTIN: scoped vence global)

**Status:** **PASS (implementação)**  
**Evidência:**

- `backend/src/core/catalog/canonical/canonical-product.repository.ts` — `findByTenantAndGtin` / `findByTenantAndFingerprintV1` com `sqlOrderScopedCanonicalFirst('canonical_products')` + `sqlCanonicalIdMatchesTenantContext`.
- `backend/src/core/catalog/canonical/canonical-product-readiness.ts` — `sqlOrderScopedCanonicalFirst`.

**Teste de integração** com duas linhas (scoped+global mesmo GTIN): **não localizado** em `backend/tests/**`. **Justificativa:** contrato verificado por inspeção de código + fase 2B PASS documentada; teste dedicado = backlog.

---

## Item 3 — **C.28** (RLS / globais visíveis em SELECT/JOIN)

**Status:** **N/A**  
**Justificativa:** `PRODUTO_PLANO_MESTRE_COMPLETO.md` cabeçalho e migrations revistas — **sem** políticas RLS em `canonical_products`. Comportamento multi-tenant assenta em queries explícitas (`sqlCanonicalIdMatchesTenantContext`), não em RLS. **Evidência:** `grep` / ausência de `ENABLE ROW LEVEL SECURITY` sobre `canonical_products` nas migrations do núcleo; alinhado a nota **C.28 (N/A)** no plano.

---

## Item 4 — **C.30** (semântica → dinheiro; `concept_ref` inválido bloqueia)

**Status:** **PASS (E2E + 5C)**  
**Evidência:**

- `backend/tests/e2e/catalog-order-ledger.e2e.test.ts` — `expect(execBody.code).not.toBe('REF_RESOLUTION_FAILED')`; fluxo até `bankTransactionService.transfer` + `transaction_created`.
- `docs/03_execution_log/2026-04-10_fase_5c.md` — contrato de erro / propagação `failureReason`.

**Actualização 2026-04-09 — C.27:** `tests/integration/canonical-resolution.test.ts` (matriz T1–T4) — ver `2026-04-09_c27_t1_t4.md`.

---

## Item 5 — **EIXO 9 / C.31** (padrão aprovado não altera `concept_ref`)

**Status:** **N/A**  
**Justificativa:** Trilho formal de «padrão aprovado» consumido em checkout **não** identificado no código de `catalog-order-ledger.e2e.test.ts` nem exigido para esta matriz mínima quando ausente (`ORIENTACAO` nota itens 5–6).

---

## Item 6 — **Contestação futura** (revogação não reescreve READY)

**Status:** **N/A**  
**Justificativa:** Trilho C.34 / contestação **não** implementado; plano permite N/A com registo explícito.

---

## Fecho

Todas as linhas da matriz **C.17.1** têm **status + evidência ou justificativa formal** (sem campos vazios).
