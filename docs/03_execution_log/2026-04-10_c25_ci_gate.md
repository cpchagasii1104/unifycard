# C.25 / C.22 — Gate local «canonical SQL patterns»

**Data:** 2026-04-10

## Âmbito

Script **`backend/scripts/gate-canonical-sql-patterns.mjs`** + comando **`pnpm run check:canonical-gates`** (workspace `unificard-backend`).

Este gate implementa **anti-regressão C.22** (padrões literais `cp.tenant_id = $<n>` e `cp.tenant_id = p.tenant_id` em `backend/src/**/*.ts`).  
O **C.25_SPEC** (`docs/02_decisions/C.25_SPEC.md`) mantém-se **rascunho** quanto a inferência / allowlist §1–3; foi acrescentada uma linha na tabela §4 desse spec a referenciar este gate, sem fechar as listas `[ ]` restantes.

## Comando

```bash
cd backend
pnpm run check:canonical-gates
```

## Resultado

**PASS** — saída `OK — no forbidden cp.tenant_id join patterns`, exit code **0**.

## Próximo passo (opcional)

- Incluir `pnpm run check:canonical-gates` num job de CI quando a pipeline do repositório estiver definida para o backend.
