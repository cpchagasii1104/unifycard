# BLOCO 1 — FAIL (FAIL FAST)

**Data:** 2026-04-10  
**Ambiente:** Windows, `DATABASE_URL` presente (migrate conectou)  
**Executor:** agente Cursor  

---

## Resultado

**STATUS: FAIL** — critério **#10** (E2E) não verificável: Jest aborta antes de correr testes.  
**Regra aplicada:** não avançar para Bloco 2; aguardar instrução humana (correcção de toolchain Jest ou dispensa explícita do critério #10).

---

## Comandos executados

```text
cd backend && pnpm install
cd backend && pnpm migrate
cd backend && pnpm run test:e2e:catalog
cd backend && node --experimental-vm-modules ./node_modules/jest/bin/jest.js --runInBand --forceExit ./tests/e2e/catalog-order-ledger.e2e.test.ts
```

Validação SQL `economic_guardianship` / `canonical_products`: scripts Node efémeros com `pg` + `dotenv` (removidos após captura de output; evidência abaixo).

---

## Erro (critério #10)

```text
TypeError: (0 , _jestUtil(...).testPathPatternToRegExp) is not a function
    at SearchSource._filterTestPathsWithStats (...@jest+core@29.7.0...)
```

**Interpretação:** falha de **infraestrutura Jest** (provável incompatibilidade entre pacotes `@jest/*` no monorepo), **não** falha semântica por scoped-only.

---

## Último estado válido — critérios 1–9

### 1A — grep (`concept-offer-refs.adapter.ts`)

| # | Verificação | Resultado |
|---|-------------|-----------|
| 1 | `fetchCategoryConceptId` | 0 ocorrências |
| 2 | `toArchitecturalRefs` | 0 ocorrências |
| 3 | `ToArchitecturalRefsInput` | 0 ocorrências |
| 4 | `categories.concept_id` | 0 ocorrências |
| 5 | `resolveConceptFromProduct` / `resolveRefsFromVariant` / `resolveRefsBatchFromVariants` | presentes |
| 6 | `canonical_product_id` | presente (comentário + JOIN) |
| 7 | Predicado READY | `INDUSTRIAL`, `concept_id IS NOT NULL`, `concept_resolution_status = 'confirmed'`, `btrim`, `category_id IS NOT NULL` |

### 1B / regra crítica — `economic_guardianship`

**`pnpm migrate`:** executou com sucesso, incluindo:

- `20260513100000_economic_guardianship_limit_amount_cents.sql`

**Colunas em `information_schema` (public.economic_guardianship):**

- `limit_amount_cents` → `bigint` (**presente**)
- **`limit_amount` — ausente** (não listada)

### 2A — `canonical_products`

- `scope`: `text`, `is_nullable = NO`
- `tenant_id`: `is_nullable = YES`
- Índices com `%gtin%` ou `%fingerprint%`: **5** nomes (≥ 4 esperado pela orientação), incluindo `uidx_canonical_gtin_global`, `uidx_canonical_gtin_scoped`, `uidx_canonical_fingerprint_global`, `uidx_canonical_fingerprint_scoped`

---

## Próximo passo humano sugerido

1. Alinhar versões Jest (`jest`, `@jest/core`, `@jest/globals`, `jest-util`) no workspace **ou**  
2. Definir por escrito se o critério #10 pode ser dispensado neste ambiente com substituto (ex.: CI apenas).

Até lá: **Bloco 2 não iniciado.**

---

## Seguimento 2026-04-10

**Dispensa formal** do critério #10 registada no plano mestre (`[!] DISPENSA CONTROLADA`); **Bloco 1 reclassificado PASS** — ver `docs/03_execution_log/2026-04-10_bloco1_PASS.md`. **Bloco 2 (2B)** autorizado.
