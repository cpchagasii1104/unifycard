# PONTEIRO/RESUMO — FEATURE CONSOLIDAÇÃO FINANCEIRA — ENCERRAMENTO (06_technical)

**Append-only. Este arquivo é um PONTEIRO/RESUMO — NÃO é idêntico ao texto canônico.** O documento canônico, com o texto integral e vinculante, vive em:
`docs/03_technical/FEATURE_CONSOLIDACAO_FINANCEIRA_CLOSURE.md`. Em caso de qualquer divergência entre este resumo e o documento canônico, **o documento canônico prevalece**.

**Status:** este arquivo (`06_technical`) e o canônico (`03_technical`) **se declaravam, os DOIS, "CANÔNICO · VINCULANTE"** — mesmo nome de arquivo, duas pastas, duas afirmações de autoridade sobre **para onde vai o dinheiro do fundo regional**. É exatamente o padrão de segunda verdade que este projeto existe para não ter. Confirmado por leitura de 1ª mão (2026-07-31): zero referência por caminho a este arquivo (`06_technical/FEATURE_CONSOLIDACAO_FINANCEIRA_CLOSURE.md`) em qualquer `.md`/`.ts` do repositório — nada o cita, nada quebra ao rebaixá-lo a ponteiro.

**Por que este perde e `03_technical` vence:** este arquivo (§ "Fonte Canônica") afirma que a conta do fundo regional é identificada por `metadata->>'systemAccountType' = 'regional_fund'`. **Isso está errado desde `3ca1df864`** (DECISION-0166 D3): a fonte real, viva no código hoje (`bank-balance-by-region.service.ts`) e no schema (`regional_fund_accounts` FK territorial `JOIN bank_accounts` `owner_type='system'`), não usa `metadata` como identidade — o que é, aliás, o padrão que Lei 7 + `SSOT_REGISTRY_UNIFICARD.md` §5.10-5.12 proíbem (identificar por string em metadata). O documento canônico (`03_technical`) **já registra a correção**, datada 2026-07-28, com nota explícita de que a frase antiga estava desatualizada.

Resumo do conteúdo: encerramento formal da feature de produto "Consolidação Financeira (READ-MODEL)" — Nível 3 (feature de produto), validada pela IA Guardiã Institucional, classificada CONFORME com ressalvas menores não bloqueantes à época (2026-01-XX). Ver o documento canônico para o texto integral, incluindo a Fonte Canônica corrigida.
