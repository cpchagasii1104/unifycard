// src/core/catalog/offer-index/offer-index.module.ts
// Módulo de índice de ofertas - READ-ONLY

import { FastifyPluginAsync } from 'fastify';
import offerIndexRoutes from './offer-index.routes';

const offerIndexModule: FastifyPluginAsync = async (fastify) => {
  await fastify.register(offerIndexRoutes);
};

export default offerIndexModule;











