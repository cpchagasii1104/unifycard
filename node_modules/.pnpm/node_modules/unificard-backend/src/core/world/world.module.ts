// src/core/world/world.module.ts
import { FastifyPluginAsync } from 'fastify';
import worldRoutes from './routes/world.routes';

const worldModule: FastifyPluginAsync = async (fastify) => {
  await fastify.register(worldRoutes);
};

export default worldModule;
















