// backend/src/core/ai/approval/approval.types.ts
export interface ApprovalRequest {
  action: string;
  payload: Record<string, any>;
  description?: string;
}

export interface Approval {
  id: string;
  userId: string;
  tenantId: string;
  token: string;
  action: string;
  payload: Record<string, any>;
  description?: string;
  status: 'pending' | 'approved' | 'rejected';
  createdAt: string;
  expiresAt: string;
}















