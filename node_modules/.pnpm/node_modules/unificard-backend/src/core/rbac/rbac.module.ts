// src/core/rbac/rbac.module.ts
import { FastifyPluginAsync } from 'fastify';
import roleRoutes from './role.routes';

const rbacModule: FastifyPluginAsync = async (fastify) => {
  await fastify.register(roleRoutes, { prefix: '/roles' });
};

export default rbacModule;
export { rbacModule };
