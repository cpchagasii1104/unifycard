// LEGACY MODULE DISABLED
// core/economy/fund está em extinção conforme SSOT_EXCLUSIVE_BANK_RULE.md
import { FastifyPluginAsync } from 'fastify';

const fundModule: FastifyPluginAsync = async (fastify) => {
  // Módulo desabilitado - nenhuma rota registrada
};

export default fundModule;
export { fundModule };
